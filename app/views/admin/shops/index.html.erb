<ul>
  <% @shops.each do |shop| %>
    <li>
      <%= link_to shop.name, admin_shop_path(shop) %> <%= link_to 'Edit', edit_admin_shop_path(shop) %> - <%= link_to 'Delete', shop, method: :delete %>
    </li>
  <% end %>
</ul>
